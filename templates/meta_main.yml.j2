---

# Generated file. Do not edit directly.
# Source template: {{ template_name }}
dependencies: []
galaxy_info:
  role_name: kernel_config
  namespace: guidugli
  author: guidugli
  company: Creuzo Tech
  description: >-
    Configure Linux kernel modules, sysctl parameters, and related udev power-management rules
    while keeping release metadata aligned with the shared Molecule platform matrix.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
{% for platform in platforms %}
    - name: {{ platform.name }}
      versions:
{%   for version in platform.versions %}
        - {{ version | tojson }}
{%   endfor %}
{% endfor %}
  galaxy_tags:
    - kernel
    - sysctl
    - linux
    - security
    - hardening
    - udev
    - modules
